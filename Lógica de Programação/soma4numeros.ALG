Algoritmo "soma4num"
Var
// Seção de Declarações das variáveis 
x, num, soma : inteiro

Inicio
// Seção de Comandos, procedimento, funções, operadores, etc... 
x <- 1
enquanto (x<=4) faca
   escreval("Digite o ", x, "° número:")
   leia(num)
   soma <- soma + num
   x<-x+1
fimenquanto
escreval ("A soma dos 4 números resulta em:",soma)


Fimalgoritmo